# Contributing

感谢你帮助改进 Go Test Tree Toggle。

## 开发流程

1. Fork 仓库并从 `main` 创建分支。
2. 运行 `npm ci` 安装依赖。
3. 修改代码并为行为变化补充测试。
4. 运行 `npm test` 和 `npm run package`。
5. 创建 Pull Request，说明动机、用户影响和验证方式。

请保持改动聚焦，不要在同一个 Pull Request 中混入无关重构。提交即表示你同意按本项目的 MIT License 提供贡献。
